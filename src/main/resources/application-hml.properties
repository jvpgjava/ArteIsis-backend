spring.datasource.url=${HML_DB_URL}
spring.datasource.username=${HML_DB_USER}
spring.datasource.password=${HML_DB_PASSWORD}

arteisis.cors.allowed-origins=${HML_CORS_ORIGINS}

server.port=${HML_SERVER_PORT:8080}

arteisis.jwt.secret=${ARTEISIS_JWT_SECRET}
arteisis.jwt.expiration-seconds=${ARTEISIS_JWT_EXPIRATION_SECONDS:86400}
